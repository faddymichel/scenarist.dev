const $ = Symbol .for;

export default class Direction {

$_producer ( play, production ) {

const direction = this;
const { player, stamp } = production;

direction .stamp = stamp;
production .setting = player ( stamp );

}

};
